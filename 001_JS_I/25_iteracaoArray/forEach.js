let gods = ['Athena', 'Zeus', 'Artemis', 'Hades', 'Apolo'];

gods.forEach(function (currentGod, index) {
  console.log(
    `God: ${currentGod}\nIndex: ${index}\n---------------------------`
  );
});

