let spaceShips = ["Tita", "Galatic", "Estrela da Morte"];

// spaceShips.push("Nave nova");

// Remove the last element from the spaceShips array
// let guardar = spaceShips.pop();

// Removes the first element from an array and returns it
// let guardar = spaceShips.shift();

// Inserts new elements at the start of an array, and returns the new length of the array.
// spaceShips.unshift("Nave nova");

// Returns the index of the first occurrence of a value in an array, or -1 if it is not present.
console.log(spaceShips.indexOf("Tita"));


